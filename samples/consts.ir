; ModuleID = 'consts'
source_filename = "consts"

declare i32 @writeln(i32)

declare i32 @readln(i32*)

define i32 @main() {
entry:
  %0 = call i32 @writeln(i32 10)
  %1 = call i32 @writeln(i32 16)
  %2 = call i32 @writeln(i32 8)
  br label %functionReturn

functionReturn:                                   ; preds = %entry
  ret i32 0
}
